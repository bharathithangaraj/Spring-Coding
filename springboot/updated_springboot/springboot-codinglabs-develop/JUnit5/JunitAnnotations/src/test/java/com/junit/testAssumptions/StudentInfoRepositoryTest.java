package com.junit.testAssumptions;

import static org.junit.Assert.*;

import org.junit.Test;

import com.junit.assertions.Device;
import com.junit.assertions.DeviceRepository;
import com.junit.assumptions.StudentInfo;
import com.junit.assumptions.StudentInfoRepository;

public class StudentInfoRepositoryTest {
	
	@Test
	public void testFindByName() {
		StudentInfoRepository studentInfoRepository = new StudentInfoRepository();
		//Device d1 = deviceRepo.addDevice(new Device("5","Mac",""));
		//StudentInfo s1=studentInfoRepository.addStudentInfo(new StudentInfo("jhon","p3e");
		//studentInfoRepository.getStudentInfo(s1);
		
	}

}
